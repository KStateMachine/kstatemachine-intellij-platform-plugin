<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.github.nsk90.kstatemachineintellijplatformplugin</id>
    <name>KStateMachine Visual</name>
    <vendor>nsk90</vendor>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <resource-bundle>messages.MyBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow factoryClass="com.github.nsk90.kstatemachineintellijplatformplugin.toolWindow.MainToolWindowFactory"
                    id="KStateMachine"
                    key="toolwindow.stripe.KStateMachine"/>
        <codeInsight.lineMarkerProvider language="kotlin"
                                        implementationClass="com.github.nsk90.kstatemachineintellijplatformplugin.psi.StateMachineLineMarkerProvider"/>
    </extensions>

    <!-- The parser is PSI-only (no K1 binding-context APIs) so it works in
         either Kotlin plugin mode. Without this declaration the Kotlin plugin
         refuses to load us in K2 mode (the default since 2025+). -->
    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>
</idea-plugin>
